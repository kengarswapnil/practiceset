class Solution {
    public int largestAltitude(int[] gain) {
        int ans = 0;
        int count = 0;
        for(int i=0;i<gain.length;i++){
            ans += gain[i];
            count = Math.max(ans,count);
        }   
        return count;
    }
}
