class Solution {
    public int rob(int[] nums) {
        int prev1 = 0, prev2 = 0, res = 0;
        for (int num : nums) {
            res = Math.max(prev2 + num, prev1);
            prev2 = prev1;
            prev1 = res;
        }
        return res;
    }
}
