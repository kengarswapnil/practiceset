class Solution {
    public int[] runningSum(int[] nums) {
        int n = nums.length;
        int ans [] = new int [n];
        for(int i=0;i<n;i++){
           ans[i] += (nums[0]+nums[i]);
        }
        return ans;
    }
}
